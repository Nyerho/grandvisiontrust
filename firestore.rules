rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // For development purposes - allow all access
    // WARNING: Remove these rules in production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}