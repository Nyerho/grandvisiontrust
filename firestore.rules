rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow admin to do everything (for development/production)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}